import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Sponsor — Revolaunch',
  description: 'Get your brand in front of thousands of founders, investors, and early adopters. Sponsor Revolaunch for targeted startup community reach.',
}

export default function SponsorLayout({ children }: { children: React.ReactNode }) {
  return children
}
